// Categorical palette (globals.css --cat-1..6) shared by every place that colors a list of
// channels/avatars/categories by index (charts, avatars, legends) — cycled in case a tenant has
// more of something than the palette has slots.
export const CATEGORICAL_COLOR_VARS = [
  "var(--color-cat-1)",
  "var(--color-cat-2)",
  "var(--color-cat-3)",
  "var(--color-cat-4)",
  "var(--color-cat-5)",
  "var(--color-cat-6)",
];
